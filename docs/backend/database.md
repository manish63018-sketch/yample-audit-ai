---
title: "Database Architecture"
---

Purpose: Define canonical DB schema, ERD, indexing, retention, and migration strategy for AuditAI. Source of truth for DB design.

See `database/schema.sql` and migration files in `database/migrations/`.
