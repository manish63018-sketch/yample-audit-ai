---
title: "Docker"
---

Purpose: Provide multi-stage Dockerfile patterns for `apps/web`, `apps/api`, and worker images, with pinned base images and build reproducibility.

Include security scanning and image signing in CI.
