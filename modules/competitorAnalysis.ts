export async function runCompetitorAnalysis(auditId: string, url: string) {
  // Placeholder: discover competitors via search or Google Maps in future
  return { ok: true, competitors: [] }
}

export default { runCompetitorAnalysis }
